/// 앱 전체 provider 의 자동 재시도 규칙 (이슈 #429).
///
/// **다시 시도하지 않는다.** Riverpod 3 은 실패한 provider 를 기본으로 간격을 늘려 가며
/// 여러 번 다시 부른다. 그동안 화면은 로딩만 보여 준다 — 인터넷이 끊긴 채 임시저장에
/// 들어가면 요청은 0.02초 만에 실패했는데 실패 화면은 약 40초 뒤에야 떴다 (실기기 실측).
///
/// 실패는 곧바로 실패 화면(`다시 시도` + 코드)으로 보여 주고, 다시 부를지는 사용자가
/// 정한다. 저절로 재시도가 꼭 필요한 provider 가 생기면 그 provider 에만 `retry:` 를
/// 따로 준다 — 전역을 다시 켜지 않는다.
Duration? elumProviderRetry(int retryCount, Object error) => null;
