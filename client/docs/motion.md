# 모션 시스템 — 이룸(ELUM)

> 애니메이션의 **duration·curve·반응 방식**을 정의한다.
> 색·타이포·간격은 [design-system.md](./design-system.md), 화면 명세는 각 flow 문서를 본다.

출처: [RomRom-FE #689 전체 애니메이션 개선](https://github.com/TEAM-ROMROM/RomRom-FE/issues/689)에서
정립한 토스 모션 철학을 이룸에 맞게 옮겼다.

## 왜 이 문서가 필요한가

디자인 시스템이 애니메이션을 다루지 않으면 **각자 알아서 하거나(제각각) 아예 안 쓴다.**
RomRom은 이걸 방치했다가 나중에 한꺼번에 고쳤다.

```
RomRom 사고 전:  Duration 14단계 분산 · Curve 6종 혼용 · 공통 눌림 위젯 없음
```

**이룸은 아직 그 상태가 아니다.** 현재 duration은 3곳(200ms)뿐이고 `Curves`는 한 번도
쓰이지 않았다. 지금 규칙을 정하면 나중에 15개 파일을 일괄 수정할 일이 없다.

이 문서는 그래서 **사후 정리가 아니라 사전 합의**다.

## 핵심 철학 — 모션은 장식이 아니라 설계

토스의 원칙을 이룸의 맥락으로 옮기면 다음과 같다.

| # | 원칙 | 이룸에서의 의미 |
| --- | --- | --- |
| 1 | **이해를 돕는 움직임** | 목적은 "예쁨"이 아니라 **상태 변화를 알아차리게** 하는 것 |
| 2 | **익숙한 패턴 우선** | 새롭고 멋진 인터랙션보다 **바로 알아보는** 인터페이스 |
| 3 | **플로우 단위 통일** | 화면 하나가 아니라 **동선 전체의 리듬**을 맞춘다 |
| 4 | **시스템화** | duration·curve를 토큰으로 관리한다. 화면에서 숫자를 쓰지 않는다 |
| 5 | **강한 인터랙션은 선택적** | 화려한 모션은 **브랜드 핵심 순간에만** |

> 토스 실패 사례: 서랍형 달력을 만들었는데 사용자가 **존재 자체를 인지하지 못했다.**
> 익숙한 탭 UI로 바꾸자 진입이 크게 늘었다.
> **"멋진 인터랙션" < "바로 알아보는 인터페이스"**

### 부드러움의 기준 — 무엇이 "부드럽다"인가 ⚠️

"부드럽게"는 취향이 아니라 **판정 가능한 조건**이다. 아래를 만족하지 못하면 다시 만든다.

| # | 기준 | 판정 |
| --- | --- | --- |
| 1 | **등속·등폭이 없다** | 같은 속도로 같은 폭을 반복하면 기계가 움직이는 것으로 읽힌다. 감쇠하거나 이징을 탄다 |
| 2 | **시작과 끝이 정확히 제자리** | 끝에서 0.1px이라도 남으면 그 자리에 굳는다. 부동소수점 오차까지 확인한다 |
| 3 | **한 번에 하나만 움직인다** | 크기·색·위치를 동시에 바꾸면 무엇이 변했는지 읽히지 않는다 |
| 4 | **되돌아오는 길이 더 느리다** | 나갈 때보다 돌아올 때가 빨라지면 튕겨 나온 것처럼 보인다 |
| 5 | **500ms를 넘지 않는다** | 넘으면 기다리게 된다. 넘겨야 한다면 그 이유를 주석에 남긴다 |

감쇠가 필요한 연출은 **물리를 그대로 쓴다.** 손으로 건드린 물체는 첫 움직임이 가장 크고
이후 절반씩 잦아들며 멎는다.

```
offset(t) = A · e^(−λt) · sin(2π·c·t)
```

`c`는 0.5의 배수여야 시작과 끝이 모두 0이 된다(기준 2). `λ`는 스윙이 절반씩 줄도록 잡는다.
값은 [AppShake](#appshake--잘못된-입력-알림)를 참조한다.

> **참고한 곳**: 토스 디자인 원칙(모션은 이해를 돕는다), Material Motion(감속 곡선),
> iOS HIG(실패 피드백은 짧고 되돌릴 수 있게).

### 이룸에만 있는 제약 — 아동 모드

이룸은 발달장애 아동이 직접 조작한다. 위 원칙에 **두 가지가 더 붙는다.**

| 제약 | 이유 |
| --- | --- |
| 아동 화면 애니메이션은 **300ms 이상** | 급격한 전환은 인지 부하가 크다 |
| 실패·오류에 **빨강·경고 아이콘 금지** | 흔들림(shake)·경고색 모션도 같은 이유로 쓰지 않는다 |
| **접근성 설정을 존중한다** | 움직임에 민감한 사용자가 있다 (아래 §접근성) |

보호자 모드는 일반 성인 기준이므로 표준 토큰을 그대로 쓴다.

## 모션 토큰 (AppMotion)

`lib/core/theme/app_motion.dart` 한 곳에서 관리한다. **화면에서 숫자를 직접 쓰지 않는다.**

### Duration — 5단계

| 토큰 | 값 | 용도 |
| --- | --- | --- |
| `instant` | 100ms | 터치 피드백 |
| `fast` | 200ms | 기본 UI 전환, 선택 상태 변경 |
| `normal` | 300ms | 카드·리스트 등장, 상태 변경 |
| `slow` | 400ms | 페이지 전환, 모달 등장 |
| `emphasis` | 500ms | 온보딩, 특수 진입 |
| `ambient` | 700ms | **배경 색이 번지는 것만** — 일과 만들기 오로라 (#380) |

`ambient`만 500을 넘는다. 500 상한의 이유는 "넘으면 기다리게 된다"인데 배경은
아무것도 막지 않는다 — 글자·버튼은 페이지 전환(`slow`)에 맞춰 이미 자리를 잡고 눌린다.
색이 전환과 함께 400에 끝나면 화면 전체가 한 번에 바뀌어 "툭" 바뀐 것으로 읽힌다.

업계 기준(Material·NN/g)은 **100~300ms**, 복잡한 움직임도 500ms를 넘기지 않는다.
"눈에 잡힐 만큼 길되, 흐름을 방해하지 않을 만큼 짧게."

> **800ms는 너무 느리다.** RomRom이 페이지 전환에 800ms를 쓰다 400ms로 줄였다.

### Curve — 4종

| 토큰 | 값 | 용도 |
| --- | --- | --- |
| `standard` | `easeInOut` | 일반 상태 변화 |
| `entry` | `easeOut` | 등장·진입 |
| `decelerate` | `easeOutCubic` | 감속 |
| `springOut` | `ElasticOutCurve(0.5)` | 버튼 눌림 복귀 |

### stagger

| 토큰 | 값 | 용도 |
| --- | --- | --- |
| `staggerDelayMs` | 30ms | 리스트 아이템이 순차 등장하는 간격 |

리스트가 **동시에 팍 뜨지 않게** 한다. `index * staggerDelayMs`로 지연을 준다.

> 아이템이 20개를 넘으면 마지막 아이템이 600ms 뒤에 뜬다. 긴 목록은 **화면에 보이는
> 앞쪽 몇 개까지만** stagger를 걸고 나머지는 즉시 표시한다.

## 공통 위젯

### AppPressable — 눌림 반응

모든 버튼·카드·아이콘의 터치 반응을 통일한다. **버튼마다 반응이 다르면 안 된다.**

```
누름  → 즉각 scale down (애니메이션 없이 setState — 토스는 "즉각 반응"이 핵심)
뗌    → SpringSimulation 물리 복귀 (살짝 bounce)
```

누를 때 애니메이션을 넣으면 **반응이 굼떠 보인다.** 즉시 줄이고, 복귀만 물리적으로 처리한다.

| 상수 | 값 | 적용 대상 |
| --- | --- | --- |
| `scaleButton` | 0.97 | 버튼 (CTA, FAB 등) |
| `scaleCard` | 0.985 | 카드·리스트 아이템 (면적이 넓다) |
| `scaleIcon` | 0.93 | 아이콘 버튼 (뒤로가기 등 작은 영역) |

면적이 넓을수록 **덜 줄인다.** 큰 카드를 0.93으로 줄이면 과장돼 보인다.

**Spring 파라미터**

```
damping ratio = damping / (2 × √(mass × stiffness))
```

1.0 미만이면 bounce가 생긴다.

- 버튼·카드: `stiffness: 300, damping: 20` → ratio ≈ 0.65 (살짝 bounce)
- 아이콘: `stiffness: 300, damping: 30` → ratio ≈ 0.87 (안정적)

**적용하지 않는 대상**

- 토글·슬라이더 — 드래그로 상태를 바꾸는 위젯이라 누름 scale이 어색하다
- PIN 키패드 — OS 시스템 키패드를 쓰므로 관여하지 않는다

**카드 안에 별도 버튼이 있을 때**

카드 전체가 `AppPressable`인데 안에 메뉴 버튼이 있으면, 메뉴를 눌러도 **카드가 같이 줄어든다.**
버튼을 `AppPressable` 바깥 형제로 빼거나 `Stack > Positioned`로 분리한다.

### AppFadeSlideIn — 등장

opacity 0→1 + 아래에서 살짝 올라오기. `delay`로 stagger를 만든다.
기본값은 `normal`(300ms) + `entry` curve.

### AppSkeleton — 로딩 → 콘텐츠

`AnimatedSwitcher` 기반으로 `fast`(200ms) fade 전환한다.

**빈 화면이나 스피너만 띄우지 않는다.** 로딩 상태는 실제 콘텐츠의 형태를 미리 보여준다.

> 단, 화면 구조가 복잡해 스켈레톤이 오히려 혼란스러우면 스피너가 낫다.
> RomRom도 홈탭은 레이어드 레이아웃이라 스켈레톤을 포기하고 스피너로 되돌렸다.

## 페이지 전환 규칙

| 상황 | 전환 | Duration |
| --- | --- | --- |
| 온보딩 단계 진행 | 수평 슬라이드 (SharedAxis) | `slow` |
| 상세 화면 진입 | 수평 슬라이드 | `slow` |
| 탭 전환 | fade | `normal` |
| 모달·바텀시트 | 아래에서 위로 + fade | `slow` |
| 시작 화면 → 홈 | fade | `slow` |
| 일과 만들기 흐름 **안** | 글자만 fade through + 8% 슬라이드, 배경은 제자리에서 색만 번짐 | 글자 `slow` · 배경 `ambient` |

### 일과 만들기 흐름 — 배경은 두고 글자만 넘어간다 (#380)

흐름(입력·보상·준비 로딩·추가질문·생성 로딩·카드확인)은 **배경 하나**(`RoutineFlowBackdrop`, 라우터의
ShellRoute)를 함께 쓴다. 화면은 바탕이 투명하다. 화면마다 배경을 그리면 색이 다른
화면(보상 — 분홍)이 **경계선째** 밀려 들어오고, 원의 움직임이 처음부터 다시 돌아 튄다.

| 무엇 | 값 | 왜 |
| --- | --- | --- |
| 배경 색 | `ambient`(700) · `standard`(easeInOut) · `TweenAnimationBuilder` | 첫 프레임에 튀지 않고 끝에서 스르르 멎는다. 도중에 되돌아가면 **지금 색에서** 방향만 바꾼다 |
| 배경이 사라질 때(카드확인) | 색은 두고 세기만 0으로 | 투명 검정과 색째 섞으면 잿빛으로 탁해진다 |
| 두 원 자리 | 입력·보상 그룹 윗변 204 ↔ 로딩·추가질문 244 — **색과 같은 곡선·같은 700ms 로** 40 옮겨 간다 | 시안이 그렇게 그렸다. 색만 번지고 원이 툭 떨어지면 거기서 끊겨 보인다 |
| 나가는 글자 | 앞 30%(120ms) 안에 흐려지며 왼쪽 8% | 둘 다 투명이라 덮이지 않는다 — 겹치면 제목 두 개가 포개진다 |
| 들어오는 글자 | 나머지 70% 동안 떠오르며 오른쪽 8%에서 | `decelerate` |
| 되돌아갈 때 | 같은 순서를 거꾸로 — `reverseCurve` + 뒤집은 곡선(`flipped`) | 그대로 두면 아래 화면이 마지막 30%에서 급히 튀어나온다 |
| 동작 줄이기 | 배경 즉시 · 글자는 미끄러지지 않고 흐려지기만 | iOS 동작 줄이기와 같은 방식 |

**화면마다 색 (2026-09-23 덤프 · #380 결정 4)** — 시안 `Gradient` 그룹의 두 원
(Eclipse 356.7 · 블러 200 / Planet 261 · 블러 100)을 그대로 옮겼고 색만 화면이 정한다.

| 화면 | 톤 | Eclipse 시작 → 끝 | Planet 시작 | 그룹 윗변 |
| --- | --- | --- | --- | --- |
| 입력 238:1643 | `input` | `#7BFFE5` → `#D16FFF` 46% | `#FCE551` | 204 |
| 보상 1082:4709 | `reward` | `#A97BFF` → `#FF6FB9` 46% | `#FB8BD4` (끝 `#FF3CA1` 0%) | 204 |
| 준비 로딩 262:4569 | `preparing` | `#CED8FF` 단색 | `#CB51FC` | 244 |
| 추가질문 262:4766 | `question` | `#A7AAFF` → `#6FC5FF` 46% | `#5651FC` | 244 |
| 생성 로딩 262:4703 | `generating` | `#7BFFB0` → `#FF6F93` 46% | `#FCF351` | 244 |
| 카드확인 262:5124 | `none` | — (세기 0) | — | 앞 화면 자리 |

Planet 아래 끝은 **투명한 색**이다(보상만 `#FF3CA1`, 나머지 `#3CFFFF`). 투명이어도
그라데이션이 그 색을 지나므로 보인다 — 노랑 → 투명 하늘의 가운데가 연두다. 하늘로
통일하면 분홍 원 가운데가 연보라로 샌다(보상 대조 3.1% → 12%).

흐름 바깥에서 들어올 때는 흐름 전체가 한 장으로 `slidePage`된다. 플랫폼 기본 페이지로 두면
iOS 뒤로 밀기가 흐름을 통째로 닫아 **`만들던 일과가 사라져요` 확인을 건너뛴다.**

**뒤로가기는 진입의 역방향이어야 한다.** 들어올 때 오른쪽에서 왔으면 나갈 때 오른쪽으로 간다.

**전환 없는 즉시 교체를 쓰지 않는다.** 화면이 툭 바뀌면 그것이 딱딱함의 원인이다.

## 접근성 — 애니메이션 끄기 ⚠️

OS 접근성 설정에서 "동작 줄이기"를 켠 사용자가 있다. 움직임이 어지럼증을 유발하는
경우가 실제로 있으며, **아동 모드에서는 더 중요하다.**

```dart
// 반복·배경 애니메이션은 이 설정을 반드시 확인한다
final reduceMotion = MediaQuery.disableAnimationsOf(context);
```

- **반복 애니메이션**(배경 그라데이션 이동 등)은 정지하고 정적 상태로 보여준다
- 컨트롤러를 만들지 않거나 `repeat()`을 걸지 않는다 — 보이지 않아도 배터리를 쓴다
- 일회성 전환은 유지해도 되지만, 길면 짧게 한다

## 성능

| 규칙 | 이유 |
| --- | --- |
| 반복 애니메이션은 `RepaintBoundary`로 격리 | 배경이 움직일 때 텍스트까지 다시 그리면 낭비다 |
| `dispose()`에서 컨트롤러를 반드시 정리 | 화면을 벗어나도 계속 돌면 배터리를 먹는다 |
| blur는 비싸다 | 큰 영역에 여러 겹 쌓지 않는다 |
| 주기를 서로 배수로 두지 않는다 | 여러 요소가 같은 자리로 돌아와 **패턴이 눈에 보인다** |

마지막 항목이 실수하기 쉽다. 20·40초로 잡으면 40초마다 정확히 겹친다. **22·28·34초**처럼
서로 나누어떨어지지 않게 잡는다.

## 금지

- `Duration(milliseconds: 250)` 처럼 **화면에서 숫자 직접 쓰기** → `AppMotion` 토큰
- `Curves.easeInOutCubicEmphasized` 같은 **토큰에 없는 curve** 임의 사용
- 전환 없는 **즉시 화면 교체**
- 아동 화면에서 **300ms 미만** 전환
- **아동 화면**에서 실패·오류에 흔들림(shake)·빨강 점멸
- 보호자 화면이라도 **등폭으로 반복하는** 흔들림 (아래 예외 참조)
- 접근성 설정을 무시한 **무한 반복 애니메이션**

### 예외 — 보호자 화면의 감쇠 흔들림

원래 이 문서는 흔들림을 전면 금지했다. 아동이 실패를 질책으로 받아들이지 않게 하려는
규칙이었고, 그 취지는 그대로다. 다만 **보호자 화면**에서는 아래 조건을 모두 지킬 때만
허용한다 (이슈 #173에서 결정).

| 조건 | 이유 |
| --- | --- |
| 아동 화면에는 쓰지 않는다 | 원래 취지 |
| 등폭이 아니라 **감쇠**한다 | 기준 1 |
| 색·아이콘을 함께 바꾸지 않는다 | 경고색 금지는 그대로 |
| 동작 줄이기가 켜지면 **흔들지 않는다** | 전정기관 민감 사용자 |
| 되돌릴 길을 남긴다 | 흔들고 끝내지 않고 다시 입력할 수 있어야 한다 |

경고색을 쓰지 않기로 한 이상, 실패를 알릴 통로가 문구뿐이면 놓치기 쉽다. 움직임이
그 자리를 대신한다 — 다만 **질책이 아니라 "다시 해보자"로 읽히는 크기**여야 한다.

> Timer·debounce 같은 **애니메이션이 아닌 Duration**은 토큰으로 바꾸지 않는다.
> `SetupDoneScreen.holdDuration`(1600ms)이 그 예다 — 화면 체류 시간이지 모션이 아니다.

## 적용 현황 (2026-07-22 갱신)

| 항목 | 상태 |
| --- | --- |
| `AppMotion` 토큰 | 적용 중 (`sceneStagger` 추가 — 장면 연출용 120ms) |
| `AppPressable` | 적용 중 |
| `AppFadeSlideIn` | **구현 완료** (`core/widgets/app_fade_slide_in.dart`) — 시작 화면 적용 |
| `AppSkeleton` | 도입 예정 |
| 페이지 전환 규칙 | **온보딩 구간 적용** (`core/router/app_transitions.dart` — slidePage/fadePage). 그 외 구간 미적용 |

> 화면 고유 안무 값(시작 화면 idle 주기 3.4s·2.6s 등)은 다른 화면과 공유되지 않고
> 배수 회피를 화면 단위로 조율해야 하므로 **AppMotion에 넣지 않고 화면의 private
> 상수로 둔다.** WHY 주석을 반드시 남긴다.

**새 화면을 만들 때 이 문서를 먼저 본다.** 기존 화면은 손대는 김에 함께 옮긴다 —
한꺼번에 고치는 작업을 만들지 않기 위해서다.
