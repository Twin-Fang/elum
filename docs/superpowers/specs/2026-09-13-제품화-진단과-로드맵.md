# 제품화 진단 — 해커톤 산출물에서 실사용 서비스로

> 2026-09-13 내부 회의에서 제기. 현재 앱은 **2일 개발 결과물**이라 데모에 최적화되어 있다.
> 실증(10월)과 실사용을 앞두고 **무엇이 mock이고 무엇이 진짜인지** 구분해 둔다.
>
> 관련: [1차 자문 회의록](../../meetings/20260913_서울ABA연구소_1차자문/) · [보상 기능 명세](./2026-09-13-보상기능과-일과-라이프사이클.md)

---

# 1. 근본 문제 — **보호자라는 주체가 없다**

소장님 자문의 핵심은 **"보호자가 설정하고 관리한다"** 였다.
그런데 현재 데이터 구조에는 **보호자 계정이 존재하지 않는다.**

```java
// Member.java — 한 덩어리다
username      // ← 아이 이름이 곧 로그인 ID
password      // ← 클라가 모든 계정에 fixedPassword를 보낸다
nickname      // 아이 호칭
character     // 아이가 고른 캐릭터
supportGoals  // 도움 목표
totalStars    // 아이가 모은 별
```

**한 `Member` = 아이 1명 + 보호자 1명이 뭉쳐 있다.** 그래서:

- 보호자가 **여러 아이**를 관리할 수 없다
- 보호자와 아이가 **다른 기기**를 쓰는 구조가 성립하지 않는다
- "보호자가 관리한다"가 **PIN으로 화면을 가리는 것** 이상이 되지 못한다

## 필요한 구조

```
현재                          필요
────                          ────
Member (아이 이름 = ID)        보호자 계정 (아이디/비밀번호)
  └ 일과                        ├ 아이 프로필 A ─ 일과
                                └ 아이 프로필 B ─ 일과
```

> 이 분리가 **자문의 "보호자가 관리한다"를 구조로 옮기는 일**이다.
> 화면을 추가하는 문제가 아니라 데이터 모델을 바꾸는 문제다.

---

# 2. 진단 — 데모에 최적화된 지점들

| # | 항목 | 현재 상태 | 실사용에서의 위험 |
| --- | --- | --- | --- |
| 1 | **인증** | `signInWithName(name)` + **`fixedPassword`** (모든 계정 동일) | **아이 호칭만 알면 남의 계정에 로그인된다.** 실증 참여자 호칭이 겹치면 데이터가 섞인다 |
| 2 | **계정 구조** | `Member` = 아이. 보호자 계정 없음 | 다자녀 불가 · 기기 분리 불가 · 보호자 권한 개념 없음 |
| 3 | **PIN** | 미설정이면 **그냥 통과** (`mode_switch_screen.dart:55`) | 온보딩을 건너뛴 상태에서 아이가 보호자 모드에 들어간다 |
| 4 | **mock 스위치** | `useMock` 기본값 **`true`** (`app_config.dart:64`) | 환경변수로 끄지 않으면 서버를 안 탄다. 배포 설정에 의존 |
| 5 | **카드 fallback** | `1차 서버 → 2차 기본 세트 → 3차 하드코딩 데모 카드` | AI 실패 시 **하드코딩 카드가 AI 결과처럼 보인다.** 실증 데이터가 오염된다 |
| 6 | **진행 상황** | "기기가 진실" — 로컬이 서버를 덮는다 | 보호자가 다른 기기에서 보면 최신이 아니다 |
| 7 | **개발자 도구** | `showDevTools` 기본 false지만 릴리스에서도 켤 수 있음 | 의도된 설계(심사자 확인용). 정식 출시 전 차단 필요 (이슈 #13) |
| 8 | **다자녀** | 없음 | 형제자매가 있는 가정을 커버 못 함 |

> **4·5번은 데모를 끝까지 성립시키려는 의도된 안전장치였다**
> (서비스 원칙 6번 — *"어떤 실패 상황에서도 데모는 끝까지 진행되어야 한다"*).
> 데모에서는 옳았지만, **실증에서는 정반대로 작동한다** — 실패를 숨기면 그 실패를 발견할 수 없다.

---

# 3. 단계별 계획

## 🔴 중간심사 (9/18) — **인증을 건드리지 않는다**

로그인이 흔들리면 전 기능이 영향받는다. D-5에 그 리스크를 질 이유가 없다.
[보상 기능 명세](./2026-09-13-보상기능과-일과-라이프사이클.md)의 13건만 한다.

## 🟠 실증 전 (10월 초) — **여기가 마지노선**

실제 사용자에게 앱을 설치하는 순간부터는 다른 문제다.

| # | 할 일 | 이유 |
| --- | --- | --- |
| 1 | **계정 분리** — `signIn(name, pin)` | 이미 받는 `guardianPin`을 자격에 포함. 같은 호칭이어도 PIN이 다르면 다른 계정. **온보딩 화면을 늘리지 않는다** |
| 2 | **PIN 미설정 통과 제거** | 개발 편의 코드가 실사용에 남아 있으면 안 된다 |
| 3 | **mock 기본값 `false`로** | 실증 빌드가 mock으로 도는 사고를 막는다 |
| 4 | **fallback 카드 표시** | 하드코딩 카드가 나왔을 때 보호자가 알 수 있게. 실증 데이터에서 제외할 수 있어야 한다 |
| 5 | 실증 빌드 점검 체크리스트 | 위 4개 + devTools off 확인 |
| 6 | 🔴 **보호자 계정 ↔ 아이 프로필 분리** | 소장님 아드님 테스트에 기기 2대가 필요하다 (3-2장) |
| 7 | 🔴 **기기 연결 (초대 코드)** | 사용자 기기는 보호자 모드에 들어갈 수 없어야 한다 |
| 8 | 🔴 **보호자 화면 서버 조회 전환** | 원격 진행 확인 |

## 🟡 본선 이후 — 구조 개편

| # | 할 일 |
| --- | --- |
| 1 | **다자녀 지원** (프로필 여러 개) |
| 2 | 보호자 완료 알림 (푸시) |
| 3 | 소셜 로그인 |

> 🔴 **보호자 계정 분리·기기 연결·원격 진행 확인은 본선이 아니라 실증 전(10월 초)으로 올라왔다.**
> 소장님이 아드님에게 직접 테스트하시려면 기기 2대가 필요하기 때문이다 — 3-2장 참고.

---

# 3-2. 실증 시나리오가 요구하는 것 — 기기 2대 🔴

> 소장님이 **아드님에게 직접 테스트**해 보고 싶다고 하셨다.
> 아드님은 성인이고 회사에 다닌다. 즉 **보호자와 사용자가 다른 기기를 쓴다.**

```
소장님 기기                      아드님 기기
──────────                      ──────────
일과 만들기 · 카드 수정      →    오늘 할 일 수행
보상 설정                         보상 확인
진행 상황 확인              ←     카드 완료
```

## 지금 구조로는 성립하지 않는다

| 문제 | 내용 |
| --- | --- |
| 계정 | 아드님 기기에 **소장님이 정한 호칭을 그대로 입력**해야 같은 계정이 된다 |
| 권한 | 그러면 아드님 기기에서도 **보호자 모드에 들어갈 수 있다** (PIN만 알면) |
| 역할 | 기기가 "보호자용인지 사용자용인지"를 구분하지 않는다 |
| 진행 | 진행 상황이 **로컬 우선**이라 소장님 기기에서 실시간 확인이 안 된다 |

## 필요한 구조 — 두 가지 사용 방식을 모두 지원한다

```
방식 A · 한 기기 (기존 유지)
  보호자 폰 하나에서 PIN으로 모드 전환
  → 어린 자녀·기기가 하나인 가정

방식 B · 두 기기 (신규) ← 소장님 케이스
  보호자 폰: 보호자 모드 전용
  사용자 폰: 사용자 모드 전용 (보호자 모드 진입 불가)
```

### 연결 방법 — 초대 코드

```
① 보호자 기기에서 아이 프로필 생성
② [기기 연결하기] → 6자리 코드 발급 (유효시간 10분)
③ 사용자 기기에서 앱 설치 → 코드 입력
④ 연결 완료 — 그 기기는 사용자 모드로 고정된다
```

| 규칙 | 내용 |
| --- | --- |
| 사용자 기기 | **보호자 모드 진입 경로가 없다.** PIN 화면 자체를 띄우지 않는다 |
| 연결 해제 | 보호자 기기에서만 가능 |
| 코드 | 6자리 숫자 · 10분 만료 · 1회용 |
| 오프라인 | 연결 후에는 오프라인에서도 수행 가능 (기존 로컬 우선 유지) |

> **왜 초대 코드인가** — 사용자가 아이디·비밀번호를 입력해야 한다면
> 발달장애인 당사자에게 진입 장벽이 된다. 보호자가 코드를 대신 입력해 줄 수도 있다.
> 로그인은 **한 번만** 있으면 되고, 그 한 번도 가장 쉬워야 한다.

## 작업 범위

| 영역 | 내용 | 추정 |
| --- | --- | --- |
| 서버 | 보호자 계정 ↔ 아이 프로필 분리 · 마이그레이션 · 초대 코드 발급/검증 · 권한 체크 | 3~4일 |
| 클라 | 회원가입·로그인 화면 · 프로필 관리 · 기기 연결 플로우 · 역할별 화면 분기 | 4~5일 |
| 동기화 | 보호자 화면은 서버 조회로 전환 (아이 화면은 로컬 우선 유지) | 1~2일 |

**합계 1.5~2주.** 중간심사 직후(9/19) 시작하면 **10월 초에 끝난다** — 실증(10월 중순) 전에 들어간다.

## 일정

```
9/18  중간심사 제출 (보상 기능 13건)
9/19  ─┐
      │ 인증·계정 구조 개편
10/5  ─┘
10/6  실증 빌드 점검 (아래 체크리스트)
10월 중순  실증 — 소장님 아드님 포함
10/28 최종 제출 (실증보고서)
```

> ⚠️ **이 일정은 중간심사가 끝나자마자 바로 시작해야 성립한다.**
> 중간심사 피드백 반영과 병행하게 되므로, 심사 직후 며칠이 가장 바쁘다.

---

# 4. 실증 빌드 체크리스트 (10월)

> 실증 직전에 이 표를 그대로 확인한다. 하나라도 어긋나면 실증 데이터를 신뢰할 수 없다.

| 확인 | 기준값 |
| --- | --- |
| ☐ `ELUM_USE_MOCK` | **false** |
| ☐ `ELUM_SHOW_DEV_TOOLS` | **false** |
| ☐ `ELUM_ENABLE_NETWORK_LOG` | false (릴리스에서 자동 차단) |
| ☐ 계정 | 참여자별로 **분리 확인** (같은 호칭 2명 테스트) |
| ☐ PIN | 미설정 통과 경로 제거 확인 |
| ☐ 카드 생성 | 서버 정상 응답 확인 (fallback으로 도는지 로그 확인) |
| ☐ 프롬프트 | 운영 DB에 최신 프롬프트 반영 확인 (관리자 페이지) |
| ☐ 서버 | `api.elum.chuseok22.com` 헬스체크 |

---

# 5. 발표에서 어떻게 말할 것인가

이 진단은 **약점이 아니라 로드맵**이다. 심사에서 "실사용 가능한가"를 물으면 이렇게 답한다.

> "현재 버전은 핵심 흐름을 검증하는 데 집중했습니다.
> 실증을 앞두고 계정 분리와 안전장치를 먼저 정리하고 있고,
> 보호자 계정과 아이 프로필을 분리하는 구조 개편을 다음 단계로 잡고 있습니다.
> 자문에서 확인한 **'보호자가 관리한다'** 는 원칙을 화면이 아니라 **데이터 구조에서** 만들려는 것입니다."

> ⚠️ **먼저 말하지는 않는다.** 묻지 않은 한계를 스스로 꺼낼 필요는 없다.
> 다만 질문을 받았을 때 **"알고 있고 계획이 있다"** 는 답이 준비되어 있어야 한다.
