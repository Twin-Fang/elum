# 이룸 (ELUM) — 프로젝트 문서

> **문서 상태: 초안 (Draft)** — 팀 브레인스토밍 결과를 정리한 러프 기획 문서.
> 확정 명세가 아니며, 구현하면서 바뀔 수 있다. 각 문서의 `미확정 사항` 섹션을 함께 볼 것.

CODEGATE 2026 AI 스타트업 해커톤 · 팀 룸룸(LUMLUM)

> 브랜치 전략: `develop`에서 작업을 모아 `main`으로 릴리스 PR을 올린다. (버전·릴리스 노트는 워크플로우가 자동 관리)

**이룸(ELUM)**: 보호자가 입력한 일과를 AI가 아동 맞춤형 **행동 카드**(이미지·음성 안내·체크리스트)로
변환해, 발달장애 아동이 스스로 다음 행동을 이해하고 수행하도록 돕는 서비스.

## 문서 구조

처음 읽는다면 01번부터 07번 순서대로 읽는 것을 추천한다.

| 문서 | 내용 | 주 독자 |
| --- | --- | --- |
| [01-overview.md](./01-overview.md) | 서비스 개요, 페인포인트, 타겟, 핵심 가치 | 전체 |
| [02-architecture.md](./02-architecture.md) | 시스템 구성, 기술 스택, 저장소 구조 | BE / AI / 인프라 |
| [03-screens.md](./03-screens.md) | 전체 화면 플로우 + 페이지별 러프 명세 | FE |
| [04-ai-card-generation.md](./04-ai-card-generation.md) | 도움 목표 기반 개인화, 프롬프트 방향, 카드 데이터 형식 | AI / BE |
| [05-ai-dlp-gateway.md](./05-ai-dlp-gateway.md) | AI DLP Gateway 보안 설계 (입력 보호 계층) | AI / 보안 |
| [06-api-spec.md](./06-api-spec.md) | REST API 러프 명세, JSON 예시, 실패 대비 fallback | FE / BE |
| [07-mvp-scope.md](./07-mvp-scope.md) | 24시간 구현 범위, 제외 목록, 데모 시나리오 | 전체 |

## 방향이 확실한 원칙 (브레인스토밍에서 합의된 것)

세부 구현은 러프하지만, 아래 방향성은 서비스의 정체성이므로 유지한다.

1. **진단명·장애 유형을 수집하지 않는다.** 개인화는 보호자가 선택한 `supportGoals`(도움 목표)로만 한다. — "최소한의 정보로 개인화"
2. **보호자 입력 원문을 LLM에 그대로 보내지 않는다.** AI DLP Gateway에서 마스킹·일반화 후 최소 정보만 전달한다. (로컬 LLM이라도 동일 적용 — 최소 정보 원칙)
3. **AI 생성 결과는 보호자 승인 후에만 아동 화면에 노출한다.** (입력 보호 + 출력 승인의 이중 안전 구조)
4. **한 카드에는 하나의 행동만 담는다.**
5. **원문을 감사 로그에 저장하지 않는다.** 탐지 유형·건수만 저장한다.
6. **데모는 어떤 실패 상황에서도 끝까지 진행되어야 한다.** AI 실패 시 fallback 체인 필수.

## 저장소 구조 (현재 / 예정)

```
elum/
├── client/   # Flutter 앱 (보호자 모드 + 아동 모드) — 생성 완료
├── server/   # Spring Boot·Java 21 서비스 서버 — AI DLP Gateway + 로컬 LLM 호출 포함 (예정)
└── docs/     # 본 문서
```

> LLM은 별도 서버를 만들지 않고 **이미 구축된 로컬 LLM**에 연결한다. (연결 정보 추후 기재)
> 초기 브레인스토밍의 Python·Flask AI 서버 안은 **폐기**됨 — DLP·프롬프트·LLM 호출 모두 Spring 내부에서 처리.

## 데모 페르소나 (전 문서 공통 예시)

- 아이 호칭: **하늘이** (실명 아님을 안내)
- 선택 도움 목표: `PREPARE_ITEMS`, `PREPARE_NEW_SITUATIONS`
- 시나리오 입력: "내일 비가 많이 올 예정이야. 아이가 학교에 갈 수 있게 준비해야 해."
- 결과: **"비 오는 날 학교 가기"** 행동 카드 5장
