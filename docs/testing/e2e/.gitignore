# pro-flutter-e2e — 밟는 과정에서 나오는 부산물은 올리지 않는다.
# 로그 덤프에는 토큰이, 계정 파일에는 자격증명이 그대로 들어 있다.

*.log
*.logcat
*.xml
*.mp4
*.trace
accounts*
credentials*
*secret*
*.env
frames/
raw/
learned.broken-*.json

# 아래는 팀이 공유해야 하므로 추적한다
!learned.json
!*.json
!README.md
